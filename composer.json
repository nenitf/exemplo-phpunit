{
    "scripts": {
        "phpunit-linux": "./vendor/bin/phpunit --colors=always",
        "phpunit-windows": "vendor\\bin\\phpunit --colors=always",
        "ltest": [ "@phpunit-linux" ],
        "wtest": [ "@phpunit-windows" ]
    },
    "autoload": {
        "psr-4":{
            "App\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^8.5"
    }
}
